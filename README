The Styler popcorn plug-in changes the look of a footnote element on the page.

The Options parameter will need:
   * Start is the time that you want this plug-in to execute
   * End is the time that you want this plug-in to stop executing 
   * Color is the color that you would like the text to appear in the target
   * Target is the id of the document element that the text needs to be attached to, this target element must exist on the DOM